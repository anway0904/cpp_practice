#include <iostream>
using namespace std;

int main(){

    // Both of the following are known as "casting operators"
    // These operators convert one datatype to another
    cout << int('a') << endl;
    cout << (int)'a' << endl;
    cout << char(65) << endl;

    return 0;
}